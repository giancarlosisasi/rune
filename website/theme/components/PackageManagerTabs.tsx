/**
 * The stock component, given a default export so `markdown.globalComponents` can
 * register it and pages need no import.
 */
export { PackageManagerTabs as default } from '@rspress/core/theme-original';
