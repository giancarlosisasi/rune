[toolchain]
channel = "1.96.1"
# CI installs whatever is named here, so a developer's `just lint` and the CI lint job
# always run the same clippy and the same rustfmt.
components = ["clippy", "rustfmt"]
